class User {
    constructor( fname, lname, username, uemail, upassword, upicture, updateAt) { 
      this.fname = fname;
      this.lname = lname;
      this.username = username;
      this.uemail = uemail;
      this.upassword = upassword;
      this.upicture = upicture;
      this.updateAt = updateAt;
    }
  }
  
export default User;